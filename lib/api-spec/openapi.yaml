openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: API specification
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: study
    description: Study coach operations
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      description: Returns server health status
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
  /profile:
    get:
      operationId: getProfile
      tags: [study]
      summary: Get the signed-in student's profile
      responses:
        "200":
          description: Profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Profile"
    patch:
      operationId: updateProfile
      tags: [study]
      summary: Update the signed-in student's profile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProfileInput"
      responses:
        "200":
          description: Updated profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Profile"
  /account:
    delete:
      operationId: deleteAccount
      tags: [study]
      summary: Permanently delete the signed-in student's account and all associated data
      responses:
        "204":
          description: Account deleted
  /tutor/conversation:
    get:
      operationId: getTutorConversation
      tags: [study]
      summary: Get the student's most recent tutor conversation
      parameters:
        - name: courseId
          in: query
          required: false
          schema: { type: string }
      responses:
        "200":
          description: Conversation history
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConversationHistory"
  /dashboard:
    get:
      operationId: getDashboard
      tags: [study]
      summary: Get the student's study dashboard
      responses:
        "200":
          description: Dashboard summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Dashboard"
  /courses:
    get:
      operationId: listCourses
      tags: [study]
      summary: List study courses
      parameters:
        - name: status
          in: query
          required: false
          schema: { type: string }
      responses:
        "200":
          description: Courses
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Course"
    post:
      operationId: createCourse
      tags: [study]
      summary: Create a study course
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CourseInput"
      responses:
        "201":
          description: Course created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Course"
  /courses/{courseId}:
    patch:
      operationId: updateCourse
      tags: [study]
      summary: Update a course's details
      parameters:
        - name: courseId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CourseUpdateInput"
      responses:
        "200":
          description: Updated course
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Course"
  /courses/{courseId}/complete:
    post:
      operationId: completeCourse
      tags: [study]
      summary: Mark a course as completed and award a completion badge
      parameters:
        - name: courseId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Completed course
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Course"
  /courses/{courseId}/archive:
    post:
      operationId: archiveCourse
      tags: [study]
      summary: Archive a course
      parameters:
        - name: courseId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Archived course
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Course"
  /courses/{courseId}/reactivate:
    post:
      operationId: reactivateCourse
      tags: [study]
      summary: Reactivate a completed or archived course
      parameters:
        - name: courseId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Reactivated course
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Course"
  /courses/{courseId}/documents:
    get:
      operationId: listCourseDocuments
      tags: [study]
      summary: List documents attached to a course
      parameters:
        - name: courseId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Documents
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DocumentSummary"
  /documents/{documentId}:
    patch:
      operationId: updateDocument
      tags: [study]
      summary: Rename or reclassify a document
      parameters:
        - name: documentId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DocumentUpdateInput"
      responses:
        "200":
          description: Updated document
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentSummary"
    delete:
      operationId: deleteDocument
      tags: [study]
      summary: Delete a document
      parameters:
        - name: documentId
          in: path
          required: true
          schema: { type: string }
      responses:
        "204":
          description: Deleted
  /documents/{documentId}/url:
    get:
      operationId: getDocumentUrl
      tags: [study]
      summary: Get a temporary download URL for a document
      parameters:
        - name: documentId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Signed URL
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentUrl"
  /documents/{documentId}/extract:
    post:
      operationId: extractSyllabus
      tags: [study]
      summary: Extract topics and dates from a syllabus document
      parameters:
        - name: documentId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Extraction result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SyllabusExtraction"
  /documents/{documentId}/confirm-extraction:
    post:
      operationId: confirmExtraction
      tags: [study]
      summary: Confirm (possibly edited) syllabus extraction results and apply them
      parameters:
        - name: documentId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SyllabusExtraction"
      responses:
        "200":
          description: Applied extraction
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ExtractionApplyResult"
  /courses/{courseId}/topics/{topicName}/study-material:
    get:
      operationId: getTopicStudyMaterial
      tags: [study]
      summary: Get the AI study guide for a topic, generating or refreshing it if stale
      parameters:
        - name: courseId
          in: path
          required: true
          schema: { type: string }
        - name: topicName
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Study guide
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TopicStudyMaterial"
  /courses/{courseId}/topics/{topicName}/study-material/regenerate:
    post:
      operationId: regenerateTopicStudyMaterial
      tags: [study]
      summary: Force-regenerate a topic's study guide even if the cached one is still fresh
      parameters:
        - name: courseId
          in: path
          required: true
          schema: { type: string }
        - name: topicName
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Study guide
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TopicStudyMaterial"
  /institutions/search:
    get:
      operationId: searchInstitutions
      tags: [study]
      summary: Search a public university/school dataset by name
      parameters:
        - name: q
          in: query
          required: true
          schema: { type: string }
        - name: countryCode
          in: query
          required: false
          schema: { type: string }
      responses:
        "200":
          description: Matching institutions
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Institution"
  /events:
    get:
      operationId: listEvents
      tags: [study]
      summary: List upcoming course events, optionally scoped to one course
      parameters:
        - name: courseId
          in: query
          required: false
          schema: { type: string }
      responses:
        "200":
          description: Events
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CourseEvent"
    post:
      operationId: createEvent
      tags: [study]
      summary: Add a calendar event to a course
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CourseEventInput"
      responses:
        "201":
          description: Event created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CourseEvent"
  /events/{eventId}:
    patch:
      operationId: updateEvent
      tags: [study]
      summary: Edit a calendar event
      parameters:
        - name: eventId
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CourseEventInput"
      responses:
        "200":
          description: Updated event
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CourseEvent"
    delete:
      operationId: deleteEvent
      tags: [study]
      summary: Delete a calendar event
      parameters:
        - name: eventId
          in: path
          required: true
          schema: { type: string }
      responses:
        "204":
          description: Deleted
  /achievements:
    get:
      operationId: listAchievements
      tags: [study]
      summary: List the student's earned badges
      responses:
        "200":
          description: Achievements
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Achievement"
  /tutor/messages:
    post:
      operationId: sendTutorMessage
      tags: [study]
      summary: Ask the AI tutor a question
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TutorMessageInput"
      responses:
        "200":
          description: Tutor response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TutorMessage"
  /quiz/active:
    get:
      operationId: getActiveQuiz
      tags: [study]
      summary: Get the student's in-progress quiz (if any), so it can be resumed
      responses:
        "200":
          description: The active quiz's next question, or an empty object when there isn't one
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActiveQuiz"
  /quiz/start:
    post:
      operationId: startQuiz
      tags: [study]
      summary: Start a new quiz for a course — one topic, or an overall mix across topics
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QuizStartInput"
      responses:
        "200":
          description: The new quiz's first question
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuizQuestion"
  /quiz/answers:
    post:
      operationId: submitQuizAnswer
      tags: [study]
      summary: Submit an answer and get feedback
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/QuizAnswerInput"
      responses:
        "200":
          description: Answer feedback
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuizFeedback"
  /courses/{courseId}/quizzes:
    get:
      operationId: listCourseQuizzes
      tags: [study]
      summary: List a course's completed quizzes, for review
      parameters:
        - name: courseId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Completed quizzes
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CompletedQuiz"
  /quiz/{quizId}/review:
    get:
      operationId: getQuizReview
      tags: [study]
      summary: Get the full question-by-question review for one completed quiz
      parameters:
        - name: quizId
          in: path
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Quiz review
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/QuizReview"
components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status
    Profile:
      type: object
      required: [id, fullName, onboardingCompleted]
      properties:
        id: { type: string }
        fullName: { type: ["string", "null"] }
        firstName: { type: ["string", "null"] }
        lastName: { type: ["string", "null"] }
        gradeLevel: { type: ["string", "null"] }
        studyMinutesPerDay: { type: ["number", "null"] }
        learningStyle: { type: ["string", "null"] }
        country: { type: ["string", "null"] }
        countryCode: { type: ["string", "null"], description: "ISO 3166-1 alpha-2 code, set only via the country picker." }
        educationLevel: { type: ["string", "null"] }
        institutionName: { type: ["string", "null"] }
        institutionCountryCode: { type: ["string", "null"] }
        institutionWebsite: { type: ["string", "null"], description: "Official site URL — present only when the institution was selected from a lookup result, i.e. verified rather than freely typed." }
        institutionDomain: { type: ["string", "null"] }
        programMajor: { type: ["string", "null"] }
        degree: { type: ["string", "null"] }
        gradeYear: { type: ["string", "null"] }
        expectedCompletionDate: { type: ["string", "null"] }
        onboardingCompleted: { type: boolean }
        personalizationEnabled: { type: boolean }
    ProfileInput:
      type: object
      properties:
        fullName: { type: string }
        firstName: { type: string }
        lastName: { type: string }
        gradeLevel: { type: string }
        studyMinutesPerDay: { type: number }
        learningStyle: { type: string }
        country: { type: string }
        countryCode: { type: ["string", "null"] }
        educationLevel: { type: string }
        institutionName: { type: string }
        institutionCountryCode: { type: ["string", "null"] }
        institutionWebsite: { type: ["string", "null"] }
        institutionDomain: { type: ["string", "null"] }
        programMajor: { type: string }
        degree: { type: string }
        gradeYear: { type: string }
        expectedCompletionDate: { type: string }
        onboardingCompleted: { type: boolean }
        personalizationEnabled: { type: boolean }
    Institution:
      type: object
      required: [name]
      properties:
        name: { type: string }
        countryCode: { type: ["string", "null"] }
        website: { type: ["string", "null"] }
        domain: { type: ["string", "null"] }
    ConversationMessage:
      type: object
      required: [role, message]
      properties:
        role: { type: string }
        message: { type: string }
    ConversationHistory:
      type: object
      required: [conversationId, messages]
      properties:
        conversationId: { type: ["string", "null"] }
        messages:
          type: array
          items: { $ref: "#/components/schemas/ConversationMessage" }
    CourseTopic:
      type: object
      required: [name, masteryLevel, masteryScore]
      properties:
        name: { type: string }
        masteryLevel: { type: string }
        masteryScore: { type: number }
    Course:
      type: object
      required: [id, name, completionDate, level, status, progress, topics]
      properties:
        id: { type: string }
        name: { type: string }
        completionDate: { type: ["string", "null"] }
        level: { type: string }
        status: { type: string }
        courseCode: { type: ["string", "null"] }
        institution: { type: ["string", "null"] }
        instructor: { type: ["string", "null"] }
        term: { type: ["string", "null"] }
        completedAt: { type: ["string", "null"] }
        progress: { type: number }
        topics:
          type: array
          items: { $ref: "#/components/schemas/CourseTopic" }
    CourseInput:
      type: object
      required: [name]
      properties:
        name: { type: string, minLength: 1 }
        completionDate: { type: string }
        level: { type: string }
        courseCode: { type: string }
        institution: { type: string }
        instructor: { type: string }
        term: { type: string }
    CourseUpdateInput:
      type: object
      properties:
        name: { type: string, minLength: 1 }
        completionDate: { type: string }
        level: { type: string }
        courseCode: { type: string }
        institution: { type: string }
        instructor: { type: string }
        term: { type: string }
    StudyTask:
      type: object
      required: [label, duration, kind, completed, courseId, courseName, topicName, quizId]
      properties:
        label: { type: string }
        duration: { type: string }
        kind: { type: string, description: "Review, Learn, or Quiz." }
        completed: { type: boolean }
        courseId: { type: string }
        courseName: { type: string }
        topicName: { type: ["string", "null"] }
        quizId: { type: ["string", "null"], description: "Set only for a Review task pointing at a specific completed quiz." }
    UpcomingEvent:
      type: object
      required: [id, courseName, type, title, eventDate]
      properties:
        id: { type: string }
        courseName: { type: string }
        type: { type: string }
        title: { type: string }
        eventDate: { type: string }
    Dashboard:
      type: object
      required: [greeting, courseName, courseProgress, strongestTopic, focusTopic, focusReason, tasks, xp, streak, questionsThisWeek, masteredTopics, upcomingEvents]
      properties:
        greeting: { type: string }
        courseName: { type: string }
        courseProgress: { type: number }
        strongestTopic: { type: string }
        focusTopic: { type: string }
        focusReason: { type: string }
        tasks:
          type: array
          items: { $ref: "#/components/schemas/StudyTask" }
        xp: { type: number }
        streak: { type: number }
        questionsThisWeek: { type: number }
        masteredTopics: { type: number }
        upcomingEvents:
          type: array
          items: { $ref: "#/components/schemas/UpcomingEvent" }
    TutorMessageInput:
      type: object
      required: [message]
      properties:
        message: { type: string, minLength: 1 }
        courseId: { type: ["string", "null"] }
        topicName: { type: ["string", "null"] }
        conversationId: { type: ["string", "null"] }
    TutorMessage:
      type: object
      required: [role, message, prompt, conversationId]
      properties:
        role: { type: string }
        message: { type: string }
        prompt: { type: string }
        conversationId: { type: string }
    QuizQuestion:
      type: object
      required: [id, quizId, courseId, courseName, number, total, topic, question, options, difficulty]
      properties:
        id: { type: string }
        quizId: { type: string }
        courseId: { type: string }
        courseName: { type: string }
        number: { type: number }
        total: { type: number }
        topic: { type: string, description: "This question's own topic — every question in an overall quiz can differ." }
        question: { type: string }
        options:
          type: array
          items: { type: string }
        difficulty: { type: string }
    ActiveQuiz:
      type: object
      description: Same shape as QuizQuestion; every property is absent when there's no active quiz to resume.
      properties:
        id: { type: string }
        quizId: { type: string }
        courseId: { type: string }
        courseName: { type: string }
        number: { type: number }
        total: { type: number }
        topic: { type: string }
        question: { type: string }
        options:
          type: array
          items: { type: string }
        difficulty: { type: string }
    QuizStartInput:
      type: object
      required: [courseId]
      properties:
        courseId: { type: string }
        topicName:
          type: ["string", "null"]
          description: Omit or pass null for an overall quiz spanning every topic in the course.
        focus:
          type: string
          description: "For an overall quiz only: \"balanced\" spreads questions evenly, \"weak-spots\" weights toward lower-mastery topics. Ignored for a topic-specific quiz."
          enum: [balanced, weak-spots]
    QuizAnswerInput:
      type: object
      required: [questionId, answer]
      properties:
        questionId: { type: string }
        answer: { type: string }
    QuizFeedback:
      type: object
      required: [correct, explanation, xp, topicName, quizCompleted, correctCount, totalQuestions]
      properties:
        correct: { type: boolean }
        explanation: { type: string }
        xp: { type: number }
        topicName: { type: string, description: "The topic this specific question covered." }
        quizCompleted: { type: boolean }
        correctCount: { type: number, description: "Running total of correct answers in this quiz so far." }
        totalQuestions: { type: number }
    CompletedQuiz:
      type: object
      required: [id, courseId, courseName, topicName, title, totalQuestions, correctCount, completedAt]
      properties:
        id: { type: string }
        courseId: { type: string }
        courseName: { type: string }
        topicName: { type: ["string", "null"], description: "Null for an overall (multi-topic) quiz." }
        title: { type: ["string", "null"] }
        totalQuestions: { type: number }
        correctCount: { type: number }
        completedAt: { type: string }
    QuizReviewQuestion:
      type: object
      required: [number, question, options, correctAnswer, explanation, selectedAnswer, correct, topicName]
      properties:
        number: { type: number }
        question: { type: string }
        options:
          type: array
          items: { type: string }
        correctAnswer: { type: string }
        explanation: { type: string }
        selectedAnswer: { type: ["string", "null"] }
        correct: { type: boolean }
        topicName: { type: ["string", "null"] }
    QuizReview:
      type: object
      required: [id, courseId, courseName, topicName, title, totalQuestions, correctCount, completedAt, questions]
      properties:
        id: { type: string }
        courseId: { type: string }
        courseName: { type: string }
        topicName: { type: ["string", "null"] }
        title: { type: ["string", "null"] }
        totalQuestions: { type: number }
        correctCount: { type: number }
        completedAt: { type: string }
        questions:
          type: array
          items: { $ref: "#/components/schemas/QuizReviewQuestion" }
    DocumentSummary:
      type: object
      required: [id, fileName, documentType, status, createdAt]
      properties:
        id: { type: string }
        fileName: { type: string }
        documentType: { type: string }
        status: { type: string }
        createdAt: { type: string }
        sizeBytes: { type: ["number", "null"] }
    DocumentUpdateInput:
      type: object
      properties:
        fileName: { type: string }
        documentType: { type: string }
    DocumentUrl:
      type: object
      required: [url]
      properties:
        url: { type: string }
    ExtractedTopic:
      type: object
      required: [name, confidence]
      properties:
        name: { type: string }
        confidence: { type: string }
    ExtractedEvent:
      type: object
      required: [type, title, eventDate, confidence]
      properties:
        type: { type: string }
        title: { type: string }
        eventDate: { type: ["string", "null"] }
        confidence: { type: string }
    SyllabusExtraction:
      type: object
      required: [confidence, courseName, courseCode, instructor, term, topics, events]
      properties:
        confidence: { type: string }
        courseName: { type: ["string", "null"] }
        courseCode: { type: ["string", "null"] }
        instructor: { type: ["string", "null"] }
        term: { type: ["string", "null"] }
        topics:
          type: array
          items: { $ref: "#/components/schemas/ExtractedTopic" }
        events:
          type: array
          items: { $ref: "#/components/schemas/ExtractedEvent" }
    ExtractionApplyResult:
      type: object
      required: [topicsCreated, eventsCreated]
      properties:
        topicsCreated: { type: number }
        eventsCreated: { type: number }
    CourseEvent:
      type: object
      required: [id, courseId, courseName, type, title, eventDate, source]
      properties:
        id: { type: string }
        courseId: { type: string }
        courseName: { type: string }
        type: { type: string }
        title: { type: string }
        eventDate: { type: string }
        description: { type: ["string", "null"] }
        source: { type: string }
    CourseEventInput:
      type: object
      required: [courseId, type, title, eventDate]
      properties:
        courseId: { type: string }
        type: { type: string }
        title: { type: string, minLength: 1 }
        eventDate: { type: string, minLength: 1 }
        description: { type: string }
    TopicKeyTerm:
      type: object
      required: [term, definition]
      properties:
        term: { type: string }
        definition: { type: string }
    TopicMaterialSource:
      type: object
      required: [fileName, chunkCount]
      properties:
        fileName: { type: string }
        chunkCount: { type: number }
    TopicStudyMaterial:
      type: object
      required: [topicName, summary, keyPoints, keyTerms, nextStep, groundedInMaterials, sources, generatedAt]
      properties:
        topicName: { type: string }
        summary: { type: string }
        keyPoints:
          type: array
          items: { type: string }
        keyTerms:
          type: array
          items: { $ref: "#/components/schemas/TopicKeyTerm" }
        nextStep: { type: string }
        groundedInMaterials:
          type: boolean
          description: True when at least one of the student's own uploaded-document chunks contributed to this guide.
        sources:
          type: array
          items: { $ref: "#/components/schemas/TopicMaterialSource" }
        generatedAt: { type: string }
    Achievement:
      type: object
      required: [id, key, title, description, courseName, courseCode, earnedAt]
      properties:
        id: { type: string }
        key: { type: string }
        title: { type: string }
        description: { type: ["string", "null"] }
        courseName: { type: ["string", "null"] }
        courseCode: { type: ["string", "null"] }
        earnedAt: { type: string }
        masteryAtCompletion: { type: ["number", "null"] }
